GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
GCS_BUCKET_NAME=your-unique-retailpulse-bucket-name
DBT_DATASET_PREFIX=retailpulse
DBT_TARGET=dev

# Optional: set this if using a service account key file.
# If omitted, the scripts can use Application Default Credentials.
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account-key.json
